[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "src/main.py",
    "src/cli.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
